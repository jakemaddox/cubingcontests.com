TZ=UTC # makes the backend use UTC as the time zone (only needed during local development)
# Mostly the same as DATABASE_URL in the CC Docker Compose file
DATABASE_URL="postgresql://$CC_DB_USERNAME.$POOLER_TENANT_ID:$CC_DB_PASSWORD@localhost:$POOLER_PROXY_PORT_TRANSACTION/$POSTGRES_DB"